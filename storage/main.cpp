#include <iostream>
#include <string>
using namespace std;

int editPerson (int personID)
{
		People person;
		if (personID != 0)
		{
				// person = openPerson (personID);
		}
		else
		{
				// Create new People
				// Set a new custom ID
		}

}

